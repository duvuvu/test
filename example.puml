@startuml
class Car {
  + make: String
  + model: String
  + year: int
  + start(): void
  + stop(): void
}

class Person {
  + name: String
  + age: int
  + drive(car: Car): void
}

Car -- Person: owns
@enduml
